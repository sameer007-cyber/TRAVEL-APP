<% if(success && success.length){ %>
    <div class="alert alert-success col-6 offset-3" role="alert"><%=success %></div>
<% }%>

<% if(failure && failure.length){ %>
    <div class="alert alert-danger col-6 offset-3" role="alert"><%=failure %></div>
<% }%>